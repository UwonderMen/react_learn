#路由切换的注意事项
```
   当路由切换的时候对应组件会重新渲染，但是渲染也要分情况
    1、之前渲染其他组件的时候，把当前组件从页面中彻底删除，再次渲染的时候，走的是第一次挂在的流程(组件挂载的生命周期流程)
    
    2、如果当前组件之前没有彻底在页面中移除(当前组件内容的子组件在切换)，只是在当前组件中，只是在基于路由切换的显示子组件，那么当前组件走的是组件更新生命周期的流程，不是走重新挂载组件的生命周期流程
```
